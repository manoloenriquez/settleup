# Supabase — all Expo public vars must be prefixed EXPO_PUBLIC_
EXPO_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# App
EXPO_PUBLIC_APP_NAME=SettleUp

# Web app URL (for share links)
EXPO_PUBLIC_WEB_URL=http://localhost:3000

# API — standalone Hono server (apps/api). Root URL (no trailing slash).
# Dev: use your Mac's LAN IP so physical devices can reach it
# EXPO_PUBLIC_API_URL=http://<mac-ip>:4000
# Prod: EXPO_PUBLIC_API_URL=https://api.settleup.app

# Optional override — defaults to `${EXPO_PUBLIC_API_URL}/ai`. Only set if
# the AI routes live somewhere different from the rest of the API.
# EXPO_PUBLIC_AI_API_URL=http://<mac-ip>:4000/ai
